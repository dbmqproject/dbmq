To install CockroachDB using Docker, see Install CockroachDB.
To run a single- or multi-node cluster on a single Docker host, see Start a Cluster. Be sure to execute all commands from within the container.
To run a multi-node cluster across multiple Docker hosts, see our docs on using orchestration tools like Kubernetes and Docker Swarm.
